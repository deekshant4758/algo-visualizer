// scripts.js


   
    const startButton = document.getElementById('start-button');
    const resetButton = document.getElementById('reset-button');
    const bubbleSortButton = document.getElementById('bubble-sort-button');
    const insertionSortButton = document.getElementById('insertion-sort-button');
    const countingSortButton = document.getElementById('counting-sort-button');
    const selectionSortButton = document.getElementById('selection-sort-button');
    const quickSortButton = document.getElementById('quick-sort-button');
    const mergeSortButton = document.getElementById('merge-sort-button');
    const linearSearchButton = document.getElementById('linear-search-button');
    const binarySearchButton = document.getElementById('binary-search-button');

    // Initialize grid and other variables...

    startButton.addEventListener('click', () => {

    });

    resetButton.addEventListener('click', () => {
        // Reset grid logic...
    });

    // bubbleSortButton.addEventListener('click', function() {
    //     const url = 'sorting.html';
    //     window.open(url,'_blank');
    // }); 

    bubblefunction(){
        window.location.href = "sorting.html";
    }

    insertionSortButton.addEventListener('click', () => {
        // Insertion Sort logic...
    });

    countingSortButton.addEventListener('click', () => {
        // Counting Sort logic...
    });

    selectionSortButton.addEventListener('click', () => {
        // Selection Sort logic...
    });

    quickSortButton.addEventListener('click', () => {
        // Quick Sort logic...
    });

    mergeSortButton.addEventListener('click', () => {
        // Merge Sort logic...
    });

    linearSearchButton.addEventListener('click', () => {
        // Linear Search logic...
    });

    binarySearchButton.addEventListener('click', () => {
        // Binary Search logic... 
    });

